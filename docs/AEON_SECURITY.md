# Security Boundary

Alpha research mode does not censor or policy-constrain cognition. Operational isolation remains external: secrets stay server-side; runtime records are append-only and signed; Supabase service-role credentials never enter browser code; cloud tables have RLS enabled with no public policies; pause/snapshot/termination remain outside AEON authority.

